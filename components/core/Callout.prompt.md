Translucent blue strip with an icon and one line of emphasis — a lightweight inline highlight.

```jsx
<Callout icon={<Icon name="zap"/>}>Расчёт проекта — менее чем за 1 час</Callout>
```

Keep it to a single line. For multiple lines or richer content, use a `Card`.
