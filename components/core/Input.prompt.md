Dark text field with a hairline border that turns blue (with a soft focus ring) on focus.

```jsx
<Input label="Email" placeholder="you@company.ru" hint="Ответим в течение часа" />
```

Pass `label` and optional `hint`. Forwards all native input attributes.
