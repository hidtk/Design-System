Small uppercase status marker (Montserrat 700).

```jsx
<Badge tone="blue">Новинка</Badge>
<Badge tone="neutral">В наличии</Badge>
```

Tones: `blue` (info), `neutral` (secondary), `alert` (rose — problems only).
