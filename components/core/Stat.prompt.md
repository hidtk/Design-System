Oversized metric numeral (light blue) dominant over a muted label. Numbers are always concrete.

```jsx
<Stat value="67 ГГц" label="Диапазон измерений" />
<Stat value="<1 ч" label="Расчёт проекта" align="center" />
```

The numeral dominates; the label is secondary. Use ranges for timelines.
