Thin line glyph from the Lucide set — brand icons are thin-stroke, no fill, blue by default.

```jsx
<Icon name="radio" size={24} />
```

Requires `<script src="https://unpkg.com/lucide@latest"></script>` on the page. Pass any Lucide `name`. Keep stroke-width ~1.6 to match the brand's thin geometry.
